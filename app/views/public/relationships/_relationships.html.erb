<% if current_user.following?(user) %>
  <%= link_to I18n.t('user.unfollow'), user_relationships_path(user.id), method: :delete, remote: true %>
<% else %>
  <%= link_to I18n.t('user.follow_us'), user_relationships_path(user.id), method: :post, remote: true %>
<% end %>